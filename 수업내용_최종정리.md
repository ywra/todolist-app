# Vibe Coding 수업 내용 최종 정리

> 본 문서는 AI 코딩 도구(Claude Code)를 활용한 todolist-app 개발 수업의 전체 과정을 날짜별로 상세 정리한 것입니다.
> **`[프롬프트]`** 표시는 수강생이 직접 입력한 프롬프트(명령)입니다.
> 작성일: 2026-04-03

---

## 2026년 3월 31일 (Day 0) — 프로젝트 생성 및 설계 문서 작성

### 1. 프로젝트 초기화
- GitHub에 `ywra/todolist-app` 리포지토리를 생성하고 로컬에 클론
- 커밋: `b689d78` Initial commit

---

### 2. 도메인 정의서 작성

> **`[프롬프트]`** 너는 최고의 비즈니스 도메인 전문가야. "todolist-app"라는 이름의 개인별 할일 관리 애플리케이션을 만들려고 해. 다음 내용을 바탕으로 적절한 서브에이전트를 활용해 도메인 정의서 초안을 작성해줘.

**business-analyst 서브에이전트**를 활용하여 `docs/1-domain-definition.md`를 작성했습니다.

**도메인 정의서 포함 내용:**
- 프로젝트 개요 (프로젝트명, 목적, 대상 사용자)
- 문제 정의 3가지 및 해결 방향
- 도메인 용어 정의 6개 (사용자, 할일, 시작일, 종료일, 완료 여부, 할일 상태)
- 핵심 도메인 모델: User(5개 속성), Todo(9개 속성)
- 할일 상태 정의 4종 (시작전/진행중/완료 실패/성공 완료) — **파생 값으로 산출** (별도 컬럼 없음)
- 유스케이스 UC-01~UC-09 (회원가입~할일 삭제)
- 비즈니스 규칙 BR-01~BR-08

---

### 3. 도메인 정의서 1차 평가

> **`[프롬프트]`** 전문 서브에이전트를 이용해 다음 5가지 항목을 기준으로 방금 작성한 도메인 정의서를 평가해줘 — 완전성, 명확성, 추적성, 검증 가능성, 유지보수성

**business-analyst 서브에이전트**가 5개 기준으로 평가:
- **총점: 19.5 / 25.0 (78%)**
- 주요 개선 과제 3가지 식별: UC-08과 BR-07 불일치, BR에 관련 UC 매핑 부재, 버전 관리 메타데이터 부재

---

### 4. 도메인 정의서 개선 (v1.1.0)

> **`[프롬프트]`** 단기 우선 개선 과제를 적용하여 도메인 정의서를 업데이트해 줘.

3가지 개선 사항 적용:
1. **변경 이력 테이블** + 문서 버전 메타데이터 추가
2. **UC-10 할일 완료 취소** 신설 (BR-07과의 불일치 해소)
3. **BR 테이블에 "관련 UC" 컬럼** 추가 + BR-09 정식 등록

---

### 5. 도메인 정의서 2차 재평가

> **`[프롬프트]`** 전문 서브에이전트를 이용해 다음 5가지 항목을 기준으로 방금 작성한 도메인 정의서를 평가해줘

**재평가 결과:**
- **총점: 21.2 / 25.0 (84.8%)** — v1.0.0 대비 **+1.7점** 향상
- 추적성 +0.5, 유지보수성 +0.8로 가장 큰 개선

---

### 6. PRD 작성을 위한 추가 정보 수집

> **`[프롬프트]`** @docs\1-domain-definition.md 를 이용해 PRD를 작성하려고 해. 추가로 필요로 하는 정보가 있다면 알려줘.

Claude가 PRD 작성에 필요한 추가 정보 5가지를 질문:
1. 기술 스택, 2. 비기능 요구사항, 3. 배포 환경, 4. UI/UX 방향성, 5. 우선순위 및 범위

---

### 7. PRD 작성

> **`[프롬프트]`** 다음 정보를 이용해서 PRD를 작성해줘. (기술 스택: React 19 + TypeScript + Zustand + TanStack Query / Node.js + Express + pg / PostgreSQL, 목표 출시일 4월3일 등 상세 정보 제공)

**business-analyst 서브에이전트**가 `docs/2-prd.md` 작성:
- 기술 스택 확정: 3-tier 아키텍처, JWT 인증, ORM 미사용(pg Raw SQL)
- 비기능 요구사항: 비밀번호 정책(8자+영문+숫자+특수문자), 데이터 제약, 페이지네이션(기본 20건)
- 3일 마일스톤: Day1 기반구축, Day2 핵심기능, Day3 통합배포

---

### 8. 사용자 시나리오 작성

> **`[프롬프트]`** 적절한 서브에이전트를 활용해 @docs\2-PRD.md 를 참조하여 사용자 시나리오를 만들어줘. docs/3-user-scenario.md 로 작성해.

**business-analyst 서브에이전트**가 `docs/3-user-scenario.md` 작성:
- SCN-01~SCN-10: UC-01~UC-10 전체 1:1 대응 시나리오
- 각 시나리오: **사전 조건 → 정상 흐름(단계별 테이블) → 예외 흐름(EX-xx-x) → 사후 조건**
- 공통 제약 사항 (인증 규칙 401/403, 상태 산출 조건, 비밀번호 정책)

---

### 9. 프로젝트 구조 설계 원칙

> **`[프롬프트]`** 적절한 서브에이전트를 사용해서 @docs\2-prd.md 문서를 바탕으로 다음 지침을 참조해서 프로젝트 구조 설계 원칙을 만들어줘. (최상위 원칙, 의존성/레이어, 코드/네이밍, 테스트/품질, 설정/보안/운영, 디렉토리 구조)

**Plan 서브에이전트**가 `docs/4-project-structure.md` 작성:
- 모노레포 구조 (frontend/ + backend/)
- 백엔드 레이어: **Route → Controller → Service → Repository** (단방향 의존)
- API 엔드포인트 10개 RESTful 정의
- 에러 응답 표준화 6종 (400/401/403/404/409/500)
- 환경 변수 목록 (백엔드 10개, 프론트엔드 1개)

> **`[프롬프트]`** 환경변수 목록을 추가해줘.

> **`[프롬프트]`** 설정 원칙에 로깅은 콘솔 로그를 사용하도록 내용을 추가해줘.

---

### 10. 기술 아키텍처 다이어그램

> **`[프롬프트]`** 적절한 서브에이전트를 사용해서 @docs\디렉토리의 문서를 바탕으로 기술 아키텍처 다이어그램을 mermaid 형식으로 작성한 후 5-arch-diagram.md 파일로 저장해줘.

**documentation-engineer 서브에이전트**가 `docs/5-arch-diagram.md` 작성:
- Mermaid 다이어그램 9개 (시스템 구조도, 레이어 흐름도, 인증 흐름도, ERD, API 흐름, FE 데이터 흐름, DB 스키마, 프로젝트 구조, 배포 흐름)

---

### 11. ERD 별도 파일 분리

> **`[프롬프트]`** ERD만 별도의 파일로 만들어줘.

`docs/6-erd.md` 생성:
- Mermaid ERD + 테이블 명세 + 인덱스 + DDL
- **핵심 수정**: status 컬럼 제거 (파생 값 원칙), due_date CHECK 제약 추가

---

### 12. 기술 스택 일관성 검토 및 수정

> **`[프롬프트]`** 적절한 서브에이전트를 사용해서 @docs\디렉토리에 작성된 파일을 분석하여 PRD에 작성된 기술 스택이 일관되게 작성되었는지를 검토해줘.

**business-analyst 서브에이전트**가 6개 문서 교차 검토 → **5건 불일치 발견**

> **`[프롬프트]`** 발견된 이슈를 해결하고 문서도 업데이트 해줘.

5건 전체 수정 완료 (status 컬럼 제거, users updated_at 제거, 세션/토큰→JWT 통일, docs 목록 보완, 토큰 저장 위치 명시)

---

### 13. DDL 파일 생성

> **`[프롬프트]`** @docs\6-erd.md 를 활용해 데이터베이스 생성을 위한 DDL 문을 작성해서 database/schema.sql 파일에 저장해줘

`database/schema.sql` 생성 (CREATE DATABASE, pgcrypto 확장, users/todos 테이블, 인덱스)

---

### 14. 실행계획서 작성

> **`[프롬프트]`** 너는 전문적인 실행계획 수립 전문가이다. @docs\ 디렉토리의 문서들을 검토하여 데이터베이스, 백엔드, 프론트엔드 단위로 해야 할 작업들을 관리가능하고 독립적인 Task로 분해하고 구체적인 실행계획을 수립한다. 적절한 서브에이전트를 활용해 병렬로 수행해줘.

**3개 Plan 서브에이전트를 병렬 실행**하여 `docs/7-execution-plan.md` 작성:
- **DB**: 7개 Task (DB-01~DB-07) — 약 4시간 40분
- **BE**: 15개 Task (BE-01~BE-15) — 약 9시간 10분
- **FE**: 14개 Task (FE-01~FE-14) — 약 19시간 30분
- **총 36개 Task**, 모든 Task에 **체크박스 완료 조건 + 의존성** 명시

---

### 15. 와이어프레임 작성

> **`[프롬프트]`** 적절한 서브에이전트를 사용해 @docs/ 디렉토리의 주요 문서를 분석한 후 와이어프레임을 8-wireframes.md 파일로 작성해줘.

**ui-designer 서브에이전트**가 `docs/8-wireframes.md` (1,016줄) 작성

> **`[프롬프트]`** 레이아웃 구조의 예시로써 데스크탑 레이아웃을 SVG 이미지로 만들어줘.

`docs/wireframes/` 디렉토리에 4개 SVG 파일 생성 (login, register, todo-list, todo-detail)

---

### 16. Swagger API 명세 작성 및 검토

> **`[프롬프트]`** 적절한 서브에이전트를 사용해 swagger spec을 작성하고 @swagger\swagger.json 파일에 저장해줘.

**api-designer 서브에이전트**가 `swagger/swagger.json` (1,323줄) 작성

> **`[프롬프트]`** @swagger\swagger.json 의 내용을 @docs\ 디렉토리의 문서와 비교분석하여 일관성을 검토하고 수정해줘.

3건 불일치 발견 및 수정 (DELETE 404 누락, PUT 날짜 예시 누락, password maxLength 누락)

---

### 17. 스타일 가이드 작성

> **`[프롬프트]`** (99designs 웹사이트 캡처 이미지 첨부) 이 캡쳐된 화면의 스타일을 분석해서 프론트엔드의 스타일 가이드를 만들어줘.

캡처 화면 분석 → `docs/9-style-guide.md` 작성:
- 디자인 철학: 클린/미니멀, 카드 기반, **모노톤+레드 포인트** (#DC3545)
- 타이포그래피: Pretendard 폰트, 7단계 스케일
- 컴포넌트 스타일 (Button 5종, Input 4상태, Card, Modal 등)
- **CSS 변수 종합** (바로 사용 가능한 전체 변수 목록)

---

## 2026년 4월 1일 (Day 1) — 데이터베이스 + 백엔드 기반 구축

### 18. PostgreSQL MCP 연결 및 DB 스키마 생성

> **`[프롬프트]`** .env의 설정을 이용해 postgresql-mcp를 이용해 로컬 데이터베이스에 연결되는지 테스트해줘

PostgreSQL MCP 서버를 통해 `SELECT NOW()` 실행 → 연결 성공 (PostgreSQL 17.9)

> **`[프롬프트]`** @docs\7-execution-plan.md 의 DB-01 Task를 진행해줘. 완료되었다면 완료조건을 충족하는지 체크해줘.

**PostgreSQL MCP 도구**로 5단계 실행:
1. `CREATE DATABASE todolist`
2. `CREATE EXTENSION IF NOT EXISTS "pgcrypto"`
3. `CREATE TABLE users` (5개 컬럼)
4. `CREATE TABLE todos` (9개 컬럼 + CHECK + FK CASCADE)
5. `CREATE INDEX idx_todos_user_id`

**MCP 도구로 제약조건/인덱스 직접 검증** → 완료 조건 **8/8 충족**

---

### 19. DB-02 ~ DB-07 백엔드 DB 레이어 구현

> **`[프롬프트]`** DB-07 Task 까지 진행해줘. 완료되었다면 완료조건을 충족하는지 체크해줘.

**backend-developer 서브에이전트**로 구현:
- DB-02: 환경 변수 설정 (`config/env.ts`, `.env.example`)
- DB-03: pg Pool 설정 (`config/db.ts`, graceful shutdown)
- DB-04: User Repository (createUser, findByEmail, findById) — Raw SQL
- DB-05: Todo Repository (CRUD + 필터/정렬/페이지네이션 **동적 SQL**)
- DB-06: updated_at **애플리케이션 레벨 처리** 결정 (트리거 미사용)
- DB-07: 통합 테스트 **16건 전체 통과** (Jest + ts-jest)

> **`[프롬프트]`** 지금까지 완료된 DB-07까지 검수리스트 체크박스에 체크해줘.

실행계획서의 DB-01~DB-07 전체 **30개 체크박스 완료 처리**

---

## 2026년 4월 2일 (Day 2) — 백엔드 API + 프론트엔드 전체 구현 + 추가 기능

### 20. BE-01 백엔드 프로젝트 초기화

> **`[프롬프트]`** `/backend-resolver` BE-01

**backend-resolver 사용자 정의 Command** 실행:
- Express 5, jsonwebtoken, bcrypt, cors 의존성 설치
- `src/app.ts` 생성 (Express 앱 + health 엔드포인트 + 404/500 핸들러)
- **20 tests 전체 통과**

---

### 21. BE-02 ~ BE-09 인증 시스템 구현

> **`[프롬프트]`** `/backend-resolver` BE-02부터 BE-04까지 다시 진행해줘.

> **`[프롬프트]`** 계속 진행해줘. (BE-05~BE-06)

> **`[프롬프트]`** 진행해줘. (BE-08~BE-09)

**backend-developer 서브에이전트**가 순차 구현:
- BE-02: env.ts + db.ts (DB 작업에서 이미 완료)
- BE-03: AppError 6종 에러 코드 + 전역 에러 핸들러
- BE-04: TypeScript 타입 정의 + express.d.ts req.user 확장
- BE-05: bcrypt hashPassword/comparePassword, JWT generateToken/verifyToken
- BE-06: JWT 인증 미들웨어 (Bearer 토큰 추출, 검증, req.user 설정)
- BE-07: User Repository (DB-04에서 완료)
- BE-08: Auth Service — register(유효성+중복+bcrypt), login(비밀번호 비교+JWT)
- BE-09: Auth Controller + Route — POST /register(201), /login(200), /logout(200)
- **84 tests 전체 통과**

---

### 22. BE-10 ~ BE-15 할일 CRUD 구현

> **`[프롬프트]`** 진행해줘. (BE-10~BE-15)

**backend-developer 서브에이전트**가 구현:
- BE-10: Todo Repository (DB-05에서 완료)
- BE-11: Todo Service — CRUD + **calculateTodoStatus** + 소유권 검증 + 페이지네이션
- BE-12: Todo Controller + Route — 7개 핸들러, authMiddleware 적용
- BE-13~15: 유효성 미들웨어 + 통합 테스트 + 단위 테스트
- **137 tests 전체 통과**

---

### 23. Swagger UI 연동 및 Express 5 타입 에러 수정

> **`[프롬프트]`** swagger.json이 적용 되었는지 확인하고 그렇지 않으면 적용해줘.

`swagger-ui-express` 설치 + app.ts에 `/api-docs` 연동

> **`[프롬프트]`** (ts-node 컴파일 에러 보고 — req.user 타입)

Express 5에서 `express.d.ts` 타입 확장이 ts-node에서 인식되지 않는 문제:
- 최종 해결: `(req as any).user` 캐스팅 방식 적용

---

### 24. 코드-문서-Swagger 일관성 검토 및 동기화

> **`[프롬프트]`** backend 디렉토리의 코드와 docs 디렉토리의 문서들, @swagger/swagger.json 파일의 일치 여부를 검토하고, 차이가 있다면 문서를 업데이트 해줘.

**business-analyst 서브에이전트**가 **12건 불일치 발견** → 전체 수정:
- 높음: DELETE 응답 200→**204**, 로그인 accessToken→**token**
- 중간: 로그아웃 메시지 통일, 로그인 400 응답 제거, 미들웨어 구조/역할 정의 수정
- 낮음: 에러 메시지 통일, Express 5 + TypeScript 버전 명시

---

### 25. FE-01 ~ FE-13 프론트엔드 전체 구현

> **`[프롬프트]`** `/frontend-resolver` FE-01

> **`[프롬프트]`** 진행해줘. (FE-02~FE-05, FE-06~FE-08, FE-09~FE-12)

**frontend-developer 서브에이전트**가 순차 구현:
- FE-01: Vite + React 19 + TypeScript 프로젝트 초기화
- FE-02: TypeScript 타입 정의 (auth, todo, api 제네릭)
- FE-03: Axios API 클라이언트 + 인터셉터 (auth 3 + todo 7)
- FE-04: Zustand 인증 스토어 (메모리 전용)
- FE-05: 유틸리티 (computeTodoStatus, 검증 함수 6종)
- FE-06: TanStack Query 커스텀 훅 (useAuth, useTodos)
- FE-07: 공통 UI 컴포넌트 9개 (Button, Input, TextArea, Modal, Pagination, Select, StatusBadge, Header, Layout)
- FE-08: React Router + PrivateRoute/PublicRoute 인증 가드
- FE-09: 회원가입 페이지 (유효성 검증, 409 중복 에러)
- FE-10: 로그인 페이지 (계정 존재 여부 비노출)
- FE-11: 할일 목록 페이지 (필터/정렬/페이지네이션/등록/완료/삭제)
- FE-12: 할일 상세 페이지 (조회/수정/완료/삭제/404)
- FE-13: 반응형 UI (360px~1280px)
- **120 tests 전체 통과**

---

### 26. 추가 기능: 사용자 정보 수정

> **`[프롬프트]`** 사용자정보를 수정하는 화면을 추가해줘.

**2개 서브에이전트 병렬 실행** (백엔드 + 프론트엔드):
- 백엔드: GET/PUT /api/auth/profile, PUT /api/auth/profile/password (157 tests)
- 프론트엔드: ProfilePage (프로필 수정 + 비밀번호 변경), /profile 라우트, Header 사용자명→프로필 링크 (125 tests)

---

### 27. 추가 기능: Dark Mode

> **`[프롬프트]`** Dark Mode 기능을 적용해줘.

**frontend-developer 서브에이전트**가 구현:
- Zustand 테마 스토어 (localStorage + 시스템 설정 감지)
- `[data-theme="dark"]` CSS 변수 오버라이드
- Header에 🌙/☀️ 토글 버튼
- **13개 CSS 파일** 하드코딩 색상 → CSS 변수로 교체
- **141 tests 전체 통과**

---

### 28. 추가 기능: 다국어 (i18n)

> **`[프롬프트]`** 다국어(한국어, 영어, 일본어) 처리 기능을 적용해줘.

**frontend-developer 서브에이전트**가 구현:
- **자체 경량 i18n** 시스템 (외부 라이브러리 없음)
- 번역 파일 3개 (ko.ts, en.ts, ja.ts)
- `useTranslation()` 훅 (dot notation + placeholder 치환)
- Header에 KO/EN/JA 언어 전환 버튼
- **전체 컴포넌트** 하드코딩 한국어 → t() 호출로 교체
- validation-utils 에러 메시지를 **번역 키**로 변경
- **174 tests 전체 통과**

---

### 29. 버그 수정: 세션 유지

> **`[프롬프트]`** Todo앱에서 새로고침하면 로그인 세션이 풀리는 버그가 있어. 수정해줘.

auth-store: `setAuth` 시 **localStorage 저장**, 앱 초기화 시 **localStorage에서 복원**

---

### 30. Chrome DevTools E2E 테스트 및 버그 수정

> **`[프롬프트]`** 현재까지 추가된 기능과 수정된 기능을 포함해서 크롬데브툴을 이용해 테스트를 한후, 버그가 있으면 수정해줘.

**Chrome DevTools MCP**로 전체 기능 테스트 → **3건 버그 발견 및 수정:**
1. **목록 페이지 날짜 ISO 문자열** → `formatDateSafe()` 함수로 포매팅
2. **상태 뱃지 다국어 미적용** → StatusBadge에 `useTranslation()` 적용
3. **필터 레이블 하드코딩** → `t('filter.label')` + 번역 키 추가

---

### 31. 로그인 에러 메시지 표시 수정

> **`[프롬프트]`** 로그인창에서 잘못된 패스워드 입력이나, 회원가입이 안되어있는 경우 알려주는 기능을 추가해줘.

**원인**: axios 응답 인터셉터가 모든 401에서 페이지 리로드 → LoginForm의 onError 미실행
**수정**: `/auth/login`, `/auth/register`의 401은 인터셉터에서 **리다이렉트 제외**

---

### 32. 오늘의 할일 기능

> **`[프롬프트]`** 매일 해야 되는 업무 목록창을 기능 추가해줘.

**2개 서브에이전트 병렬 실행**:
- 백엔드: GET /api/todos/daily (오늘 날짜 범위 조회)
- 프론트엔드: DailyTodoPage + Header 네비게이션 + 다국어

> **`[프롬프트]`** 오늘의 할일에서 목록을 추가하는 기능이 없어. 추가해줘.

DailyTodoPage에 **등록 모달** 추가, 시작일/종료일 **오늘 날짜 자동 설정**

---

### 33. DATE 타입 UTC 변환 버그 수정

오늘의 할일 등록 시 **500 에러** 발생 → 원인: pg 드라이버가 DATE를 Date 객체로 변환 → UTC 타임존 밀림
**수정**: `db.ts`에 `types.setTypeParser(1082, val => val)` 추가 → YYYY-MM-DD 문자열 유지

---

### 34. 보상 시스템

> **`[프롬프트]`** 오늘의 할일을 마무리한 갯수가 10개, 30, 100개 쌓이면 보상이 있는 구조로 구현 필요. 할일을 마무리한 갯수에 따른 보상은 작은 보상, 중간 보상, 큰 보상으로 구분해서 보상 획득. 보상의 내용은 내가 작성가능해야함. UI 에서 다음 목표까지 얼만큼의 할일을 해결해야 할지 보드 게임 같은 말판으로 보여줘도 좋아 보임

**2개 서브에이전트 병렬 실행**:
- 백엔드: rewards 테이블, GET/POST/PUT /api/rewards, completeTodo 시 자동 달성
- 프론트엔드: RewardPage **보드게임 말판 UI** (0→10→30→100 구간, 🏃 캐릭터, 골드 카드)

**Chrome DevTools로 10/30/100 마일스톤 전체 검증 완료**

---

### 35. 오늘의 할일 독립 분리

> **`[프롬프트]`** 내할일 목록과 오늘의 할일은 연동이 안되고 전혀 다른 목록으로 관리하고 싶어. 물론 보상현황은 오늘의 할일과 연동돼야해.

**2개 서브에이전트 병렬 실행**:
- 백엔드: `daily_todos` 테이블 신규, /api/daily-todos API 5개, reward 카운트를 daily_todos 기준으로 변경
- 프론트엔드: DailyTodoItem 컴포넌트, useDailyTodos 훅, API 분리

> **`[프롬프트]`** 내할일 목록도 오늘의 할일과 보상현황처럼 좌측상단에서 클릭할 수 있었으면 해.

Header 네비게이션에 **내 할일 목록 | 오늘의 할일 | 보상 현황** 3개 링크 추가

---

### 36. 오늘의 할일 달력 기능

> **`[프롬프트]`** 오늘의 할일에서 완료사항을 달력에서 완료 표시하고, 미완료된 표시가 있는 날짜를 달력에서 클릭하면 성공완료와 미완료 목록을 볼수 있게 수정해줘.

**frontend-developer 서브에이전트**가 구현:
- 백엔드: GET /api/daily-todos/calendar?year=&month= (월별 조회)
- 프론트엔드: **DailyCalendar 컴포넌트** (7열 CSS Grid, 월 이동, 오늘 하이라이트)
- 날짜별 마커: **초록●(모두 완료) / 빨간●(미완료 있음)**
- 날짜 클릭 → 해당 날짜 할일 목록 표시

---

### 37. 문서 동기화 및 README 생성

> **`[프롬프트]`** 프론트엔드 개발 도중 변경된 백엔드의 설계 사항을 포함해서, 프로젝트내 모든 관련 문서들을 업데이트해줘. 그리고, Readme.md 파일도 생성해줘.

**documentation-engineer 서브에이전트**가 전체 문서 v1.2.0 동기화 + README.md 생성

---

### 38. SCN-01~SCN-17 전체 E2E 통합 테스트

> **`[프롬프트]`** playwright-mcp를 이용해 수정된 @docs/3-user-scenario.md를 참조해 통합테스트를 수행해줘.

**Playwright MCP**로 SCN-01~SCN-17 전체 테스트 → **17/17 PASS**, 스크린샷 7장 + 보고서 저장

---

## 2026년 4월 3일 (Day 3) — 배포 및 최종 검증

### 39. Supabase 마이그레이션

> **`[프롬프트]`** postgresql-mcp와 supabasemcp를 이용해 로컬 데이터베이스의 스키마를 supabase로 마이그레이션 해줘

**Supabase MCP**로 `apply_migration` 실행 → 4개 테이블 + 3개 인덱스 생성 완료

---

### 40. Vercel 배포 설정 및 트러블슈팅

> **`[프롬프트]`** (Vercel FUNCTION_INVOCATION_FAILED 에러 스크린샷)

**배포 이슈 5건 수정:**
1. **npm peer dependency 충돌** → `.npmrc` (`legacy-peer-deps=true`)
2. **Vercel Serverless 설정** → `api/index.ts` + `vercel.json` 추가
3. **Swagger UI 안 보임** → CDN 에셋으로 변경 (Serverless 호환)
4. **Swagger 서버 URL** → `"url": "/"` 추가 (현재 호스트 자동 사용)
5. **프론트엔드 빌드 에러 35개** → tsconfig 테스트 제외, enum→const object, vitest reference 추가

---

### 41. CORS 에러 해결

> **`[프롬프트]`** 회원가입이 안되는것 같어. 크롬데브툴을 이용하여 전체 기능을 다시 검증해줘.

**Playwright MCP**로 배포 사이트 검증 → **CORS 에러 발견**:
- `CORS_ORIGIN`이 `http://localhost:5173`으로 설정됨
- Vercel Dashboard에서 `https://frontend-ywra.vercel.app`으로 변경 + Redeploy 안내

---

### 42. GitHub Actions CI/CD

GitHub Actions 워크플로우 추가:
- `backend-prod.yaml`: 백엔드 배포
- `frontend-prod.yaml`: 프론트엔드 배포

---

### 43. 회원가입 비밀번호 재확인

> **`[프롬프트]`** 프런트엔트화면에서 회원가입시 비밀번호 재확인하는 기능을 추가해줘.

RegisterForm에 `confirmPassword` 필드 추가, 불일치 시 에러 메시지 표시

---

## 프로젝트 최종 현황

### 커밋 히스토리 (37개)

| 날짜 | 커밋 수 | 주요 내용 |
|------|---------|-----------|
| 03/31 | 1 | Initial commit |
| 04/01 | 5 | 설계 문서 9종 + DB 스키마 + DB 레이어 구현 |
| 04/02 | 21 | BE/FE 전체 구현 + 추가 기능 6개 + 버그 수정 + E2E 테스트 |
| 04/03 | 10 | Vercel 배포 + Supabase 마이그레이션 + 빌드 수정 + CI/CD |

### 테스트 현황

| 영역 | 프레임워크 | 테스트 수 |
|------|-----------|-----------|
| 백엔드 | Jest + ts-jest + Supertest | **188건** |
| 프론트엔드 | Vitest + React Testing Library | **197건** |
| E2E | Playwright MCP | **17건** (SCN-01~SCN-17) |
| **합계** | | **402건 전체 통과** |

### 생성된 문서 (9종 + README)

| 문서 | 파일명 | 최종 버전 |
|------|--------|-----------|
| 도메인 정의서 | docs/1-domain-definition.md | v1.2.0 |
| PRD | docs/2-prd.md | v1.2.0 |
| 사용자 시나리오 | docs/3-user-scenario.md | v1.1.0 |
| 프로젝트 구조 설계 | docs/4-project-structure.md | v1.2.0 |
| 아키텍처 다이어그램 | docs/5-arch-diagram.md | v1.2.0 |
| ERD | docs/6-erd.md | v1.1.0 |
| 실행계획서 | docs/7-execution-plan.md | v1.1.0 |
| 와이어프레임 | docs/8-wireframes.md | v1.0.0 |
| 스타일 가이드 | docs/9-style-guide.md | v1.0.0 |
| README | README.md | - |
| Swagger | swagger/swagger.json | v1.2.0 |

### API 엔드포인트 (24개)

| 그룹 | 엔드포인트 수 |
|------|-------------|
| 인증 & 프로필 | 6개 (register, login, logout, profile CRUD, password) |
| 내 할일 목록 | 7개 (CRUD + complete/incomplete) |
| 오늘의 할일 | 6개 (CRUD + complete/incomplete + calendar) |
| 보상 | 3개 (progress, create, update) |
| 기타 | 2개 (health, api-docs) |

### 기술 스택

| 레이어 | 기술 |
|--------|------|
| 프론트엔드 | React 19 + TypeScript + Zustand + TanStack Query + Vite |
| 백엔드 | Node.js + Express 5 + TypeScript + pg (Raw SQL) |
| 데이터베이스 | PostgreSQL 17 (로컬) + Supabase (클라우드) |
| 인증 | JWT (Bearer Token, localStorage 영속화) |
| 배포 | Vercel (Serverless) + GitHub Actions CI/CD |

### 활용한 서브에이전트 (7종)

| 서브에이전트 | 활용 장면 |
|-------------|-----------|
| **business-analyst** | 도메인 정의서, PRD, 시나리오, 일관성 검토 |
| **Plan** | 프로젝트 구조 설계, 실행계획 수립 |
| **ui-designer** | 와이어프레임 |
| **documentation-engineer** | 아키텍처 다이어그램, 문서 동기화 |
| **api-designer** | Swagger 명세, API 일관성 검토 |
| **backend-developer** | DB 레이어, 백엔드 API 전체 |
| **frontend-developer** | 프론트엔드 전체, Dark Mode, 다국어, 보상 UI, 달력 |

### 활용한 MCP 도구 (5종)

| MCP 도구 | 용도 |
|---------|------|
| **postgresql-mcp** | 로컬 DB 스키마 생성, 쿼리 검증 |
| **supabase-mcp** | 클라우드 DB 마이그레이션 |
| **chrome-devtools-mcp** | 브라우저 E2E 테스트, 버그 발견 |
| **playwright-mcp** | 배포 사이트 E2E 테스트, 스크린샷 |
| **context7-mcp** | 라이브러리 문서 참조 |
