# Vibe Coding 수업 내용 정리

> 본 문서는 AI 코딩 도구(Claude Code)를 활용한 todolist-app 개발 수업의 전체 과정을 날짜별로 정리한 것입니다.
> **`[프롬프트]`** 표시는 수강생이 직접 입력한 프롬프트(명령)입니다.

---

## 2026년 3월 31일 (Day 0) — 프로젝트 생성 및 설계 문서 작성

### 1. 프로젝트 초기화

GitHub에 `todolist-app` 리포지토리를 생성하고 로컬에 클론하여 프로젝트를 시작했습니다.

- 커밋: `b689d78` Initial commit

---

### 2. 도메인 정의서 작성

> **`[프롬프트]`** 너는 최고의 비즈니스 도메인 전문가야. "todolist-app"라는 이름의 개인별 할일 관리 애플리케이션을 만들려고 해. 다음 내용을 바탕으로 적절한 서브에이전트를 활용해 도메인 정의서 초안을 작성해줘.

**business-analyst 서브에이전트**를 활용하여 `docs/1-domain-definition.md` 를 작성했습니다.

**도메인 정의서 포함 내용:**
- 프로젝트 개요 (프로젝트명, 목적, 대상 사용자)
- 문제 정의 3가지 및 해결 방향
- 도메인 용어 정의 6개 (사용자, 할일, 시작일, 종료일, 완료 여부, 할일 상태)
- 핵심 도메인 모델: User(5개 속성), Todo(9개 속성)
- 할일 상태 정의 4종 (시작전/진행중/완료 실패/성공 완료) — 파생 값으로 산출
- 유스케이스 UC-01~UC-09 (회원가입~할일 삭제)
- 비즈니스 규칙 BR-01~BR-08

---

### 3. 도메인 정의서 평가 (1차)

> **`[프롬프트]`** 전문 서브에이전트를 이용해 다음 5가지 항목을 기준으로 방금 작성한 도메인 정의서를 평가해줘 — 완전성, 명확성, 추적성, 검증 가능성, 유지보수성

**business-analyst 서브에이전트**가 5개 기준으로 평가한 결과:
- **총점: 19.5 / 25.0 (78%)**
- 주요 개선 과제: UC-08과 BR-07 불일치, BR에 관련 UC 매핑 부재, 버전 관리 메타데이터 부재

---

### 4. 도메인 정의서 개선 (v1.1.0)

> **`[프롬프트]`** 단기 우선 개선 과제를 적용하여 도메인 정의서를 업데이트해 줘.

3가지 개선 사항 적용:
1. **변경 이력 테이블** + 문서 버전 메타데이터 추가
2. **UC-10 할일 완료 취소** 신설 (BR-07과의 불일치 해소)
3. **BR 테이블에 "관련 UC" 컬럼** 추가 + BR-09 정식 등록

---

### 5. 도메인 정의서 재평가 (2차)

> **`[프롬프트]`** 전문 서브에이전트를 이용해 다음 5가지 항목을 기준으로 방금 작성한 도메인 정의서를 평가해줘

**재평가 결과:**
- **총점: 21.2 / 25.0 (84.8%)** — v1.0.0 대비 +1.7점 향상
- 추적성 +0.5, 유지보수성 +0.8로 가장 큰 개선

---

### 6. PRD (Product Requirements Document) 작성

> **`[프롬프트]`** @docs\1-domain-definition.md 를 이용해 PRD를 작성하려고 해. 추가로 필요로 하는 정보가 있다면 알려줘.

Claude가 PRD 작성에 필요한 추가 정보 5가지를 질문:
1. 기술 스택
2. 비기능 요구사항
3. 배포 환경
4. UI/UX 방향성
5. 우선순위 및 범위

> **`[프롬프트]`** 다음 정보를 이용해서 PRD를 작성해줘. (기술 스택: React 19 + TypeScript + Zustand + TanStack Query / Node.js + Express + pg / PostgreSQL, 3일 일정 등 상세 정보 제공)

**business-analyst 서브에이전트**가 `docs/2-prd.md` 작성:
- 기술 스택 확정 (3-tier 아키텍처, JWT 인증)
- 비기능 요구사항 (비밀번호 정책, 데이터 제약, 페이지네이션)
- 3일 마일스톤 (Day1 기반구축, Day2 핵심기능, Day3 통합배포)

---

### 7. 사용자 시나리오 작성

> **`[프롬프트]`** 적절한 서브에이전트를 활용해 @docs\2-PRD.md 를 참조하여 사용자 시나리오를 만들어줘. docs/3-user-scenario.md 로 작성해.

**business-analyst 서브에이전트**가 `docs/3-user-scenario.md` 작성:
- SCN-01~SCN-10: UC-01~UC-10 전체 1:1 대응 시나리오
- 각 시나리오: 사전 조건 → 정상 흐름(테이블) → 예외 흐름(EX-xx-x) → 사후 조건
- 공통 제약 사항 (인증 규칙, 상태 산출 조건, 비밀번호 정책)

---

### 8. 프로젝트 구조 설계 원칙

> **`[프롬프트]`** 적절한 서브에이전트를 사용해서 @docs\2-prd.md 문서를 바탕으로 다음 지침을 참조해서 프로젝트 구조 설계 원칙을 만들어줘. (최상위 원칙, 의존성/레이어, 코드/네이밍, 테스트/품질, 설정/보안/운영, 디렉토리 구조)

**Plan 서브에이전트**가 `docs/4-project-structure.md` 작성:
- 모노레포 구조 (frontend/ + backend/)
- 백엔드 레이어: Route → Controller → Service → Repository (단방향)
- 프론트엔드 레이어: Page → Component → hooks/api → stores
- 네이밍 규칙 (kebab-case, PascalCase, camelCase, snake_case)
- API 엔드포인트 10개 RESTful 정의
- 에러 응답 표준화 6종 코드
- 환경 변수 목록 (백엔드 10개, 프론트엔드 1개)

> **`[프롬프트]`** 환경변수 목록을 추가해줘.

백엔드/프론트엔드 환경변수를 테이블 형태(변수명, 설명, 필수, 기본값, 예시)로 추가.

> **`[프롬프트]`** 설정 원칙에 로깅은 콘솔 로그를 사용하도록 내용을 추가해줘.

§5.5 로깅 섹션 추가 (console.log/warn/error 수준별 구분).

---

### 9. 기술 아키텍처 다이어그램

> **`[프롬프트]`** 적절한 서브에이전트를 사용해서 @docs\디렉토리의 문서를 바탕으로 기술 아키텍처 다이어그램을 mermaid 형식으로 작성한 후 5-arch-diagram.md 파일로 저장해줘. 최대한 단순한 구조로 표현해야 해.

**documentation-engineer 서브에이전트**가 `docs/5-arch-diagram.md` 작성:
- Mermaid 다이어그램 9개 (시스템 구조도, 레이어 흐름도, 인증 흐름도, ERD, API 흐름, FE 데이터 흐름, DB 스키마, 프로젝트 구조, 배포 흐름)

---

### 10. ERD 별도 파일 분리

> **`[프롬프트]`** ERD만 별도의 파일로 만들어줘.

`docs/6-erd.md` 생성:
- Mermaid ERD 다이어그램
- 테이블 명세 (users, todos 컬럼별 상세)
- 인덱스 정의
- DDL (CREATE TABLE SQL)
- **주요 수정**: status 컬럼 제거 (파생 값 원칙), due_date CHECK 제약 추가

---

### 11. 기술 스택 일관성 검토

> **`[프롬프트]`** 적절한 서브에이전트를 사용해서 @docs\디렉토리에 작성된 파일을 분석하여 PRD에 작성된 기술 스택이 일관되게 작성되었는지를 검토해줘.

**business-analyst 서브에이전트**가 6개 문서 전체를 교차 검토:

| 불일치 # | 심각도 | 내용 |
|----------|--------|------|
| 1 | 높음 | 5-arch-diagram.md에 status 컬럼이 저장 컬럼으로 존재 |
| 2 | 중간 | 5-arch-diagram.md users에 updated_at 존재 |
| 3 | 낮음 | 1-domain-definition.md "세션/토큰" 혼용 |
| 4 | 낮음 | 5-arch-diagram.md docs 목록에 6-erd.md 누락 |
| 5 | 낮음 | 인증 흐름도에 localStorage 근거 없음 |

> **`[프롬프트]`** 발견된 이슈를 해결하고 문서도 업데이트 해줘.

5개 불일치 항목 전체 수정 완료.

---

### 12. DDL 파일 생성

> **`[프롬프트]`** @docs\6-erd.md 를 활용해 데이터베이스 생성을 위한 DDL 문을 작성해서 database/schema.sql 파일에 저장해줘

`database/schema.sql` 생성 (CREATE DATABASE, pgcrypto 확장, users/todos 테이블, 인덱스).

---

### 13. 실행계획서 작성

> **`[프롬프트]`** 너는 전문적인 실행계획 수립 전문가이다. @docs\ 디렉토리의 문서들을 검토하여 데이터베이스, 백엔드, 프론트엔드 단위로 해야 할 작업들을 관리가능하고 독립적인 Task로 분해하고 구체적인 실행계획을 수립한다. 적절한 서브에이전트를 활용해 병렬로 수행해줘.

**3개 Plan 서브에이전트를 병렬 실행**하여 `docs/7-execution-plan.md` 작성:
- **DB**: 7개 Task (DB-01~DB-07) — 약 4시간 40분
- **BE**: 15개 Task (BE-01~BE-15) — 약 9시간 10분
- **FE**: 14개 Task (FE-01~FE-14) — 약 19시간 30분
- 총 36개 Task, 일자별 실행 계획 (Day1~Day3)
- 모든 Task에 체크박스 형태 완료 조건 + 의존성 명시

---

### 14. 와이어프레임 작성

> **`[프롬프트]`** 적절한 서브에이전트를 사용해 @docs/ 디렉토리의 주요 문서를 분석한 후 와이어프레임을 8-wireframes.md 파일로 작성해줘.

**ui-designer 서브에이전트**가 `docs/8-wireframes.md` (1,016줄) 작성:
- 화면 전환 흐름도 (Mermaid)
- 공통 컴포넌트 6종 (Header, StatusBadge, Modal, FormField, Pagination, Toast)
- 4개 페이지 상세 (LoginPage, RegisterPage, TodoListPage, TodoDetailPage)
- 각 페이지: ASCII 레이아웃, UI 요소 목록, 인터랙션 흐름(Mermaid), 에러 표시, 반응형 차이

> **`[프롬프트]`** 레이아웃 구조의 예시로써 데스크탑 레이아웃을 SVG 이미지로 만들어줘.

`docs/wireframes/` 디렉토리에 4개 SVG 파일 생성:
- `login-page.svg`, `register-page.svg`, `todo-list-page.svg`, `todo-detail-page.svg`

---

### 15. Swagger API 명세 작성

> **`[프롬프트]`** 적절한 서브에이전트를 사용해 @docs\2-prd.md, @docs\3-user-scenario.md, @docs\6-erd.md, @docs\5-arch-diagram.md 파일의 정보를 분석하여 swagger spec을 작성하고 @swagger\swagger.json 파일에 저장해줘.

**api-designer 서브에이전트**가 `swagger/swagger.json` (1,323줄) 작성:
- OpenAPI 3.0.3, 10개 엔드포인트
- 11개 스키마, 4개 공통 응답, JWT Bearer 인증
- 모든 에러 응답 케이스 포함

> **`[프롬프트]`** 적절한 서브에이전트를 이용해 @swagger\swagger.json 의 내용을 @docs\ 디렉토리의 문서와 비교분석하여 일관성을 검토하고 수정해줘.

3건 불일치 발견 및 수정 (DELETE 404 누락, PUT 날짜 형식 예시 누락, password maxLength 누락).

---

### 16. 첫 번째 커밋 및 푸시

> **`[프롬프트]`** 현재 시점까지 커밋 후 원격리포지토리에 푸시해줘.

커밋 `1633413`: docs 7개 + database/schema.sql (8개 파일)
이후 추가 커밋: .claude/, .qwen/ 설정, 와이어프레임/SVG/Swagger, mockup 디렉토리

---

## 2026년 4월 1일 (Day 1) — 데이터베이스 + 백엔드 기반 구축

### 17. PostgreSQL MCP 연결 테스트

> **`[프롬프트]`** .env의 설정을 이용해 postgresql-mcp를 이용해 로컬 데이터베이스에 연결되는지 테스트해줘

PostgreSQL MCP 서버를 통해 `SELECT NOW()` 실행 → 연결 성공 확인 (PostgreSQL 17.9).

---

### 18. DB-01 스키마 생성

> **`[프롬프트]`** @docs\7-execution-plan.md 의 DB-01 Task를 첫번째 단계인 Postgresql 데이터베이스 및 스키마의 기본 설정을 진행해줘. 완료되었다면 완료조건을 충족하는지 체크해줘.

**PostgreSQL MCP 도구**를 사용하여 5단계 실행:
1. `CREATE DATABASE todolist`
2. `CREATE EXTENSION IF NOT EXISTS "pgcrypto"`
3. `CREATE TABLE users` (5개 컬럼)
4. `CREATE TABLE todos` (9개 컬럼 + CHECK + FK CASCADE)
5. `CREATE INDEX idx_todos_user_id`

완료 조건 8/8 충족 확인 (MCP 도구로 테이블/제약조건/인덱스 검증).

---

### 19. DB-02 ~ DB-07 백엔드 DB 레이어 구현

> **`[프롬프트]`** DB-07 Task 까지 진행해줘. 완료되었다면 완료조건을 충족하는지 체크해줘.

**backend-developer 서브에이전트**로 구현:
- DB-02: 환경 변수 설정 (`config/env.ts`, `.env.example`)
- DB-03: pg Pool 설정 (`config/db.ts`, graceful shutdown)
- DB-04: User Repository (createUser, findByEmail, findById)
- DB-05: Todo Repository (CRUD + 필터/정렬/페이지네이션 동적 SQL)
- DB-06: updated_at 애플리케이션 레벨 처리 결정
- DB-07: 통합 테스트 16건 전체 통과 (Jest + ts-jest)

> **`[프롬프트]`** 지금까지 완료된 DB-07까지 검수리스트 체크박스에 체크해줘.

실행계획서의 DB-01~DB-07 전체 30개 체크박스 완료 처리.

---

## 2026년 4월 2일 (Day 2) — 백엔드 API + 프론트엔드 전체 구현

### 20. BE-01 백엔드 프로젝트 초기화

> **`[프롬프트]`** `/backend-resolver` BE-01

**backend-resolver 사용자 정의 Command** 실행:
- Express 5, jsonwebtoken, bcrypt, cors 의존성 설치
- `src/app.ts` 생성 (Express 앱 + health 엔드포인트 + 404/500 핸들러)
- nodemon 개발 서버, supertest 테스트 설정
- **20 tests 전체 통과**

---

### 21. BE-02 ~ BE-04 환경설정/타입/미들웨어

> **`[프롬프트]`** `/backend-resolver` BE-02부터 BE-04까지 다시 진행해줘.

DB 작업에서 이미 구현된 코드를 확인하고, 누락된 테스트 보강:
- BE-02: env.ts + db.ts (DB 작업에서 완료)
- BE-03: AppError 6종 에러 코드 + 전역 에러 핸들러 (BE-01에서 구현)
- BE-04: TypeScript 타입 정의 + express.d.ts req.user 확장
- **47 tests 전체 통과**, tsc --noEmit 컴파일 에러 없음

---

### 22. BE-05 ~ BE-06 유틸리티/인증 미들웨어

> **`[프롬프트]`** 계속 진행해줘.

- BE-05: bcrypt hashPassword/comparePassword, JWT generateToken/verifyToken
- BE-06: JWT 인증 미들웨어 (Bearer 토큰 추출, 검증, req.user 설정)
- auth-middleware 단위 테스트 15개 신규 작성
- **64 tests 전체 통과**

---

### 23. BE-08 ~ BE-09 Auth Service/Controller/Route

> **`[프롬프트]`** 계속 진행해줘.

**backend-developer 서브에이전트**가 구현:
- Auth Service: register(유효성 검증 + 중복 체크 + bcrypt 해싱), login(비밀번호 비교 + JWT 발급)
- Auth Controller: POST /register(201), /login(200), /logout(200)
- Auth Routes: authMiddleware 적용
- 단위 테스트 11개 + 통합 테스트 9개
- **84 tests 전체 통과**

---

### 24. BE-10 ~ BE-12 Todo Service/Controller/Route

> **`[프롬프트]`** 진행해줘.

**backend-developer 서브에이전트**가 구현:
- Todo Service: CRUD + calculateTodoStatus + 소유권 검증 + 페이지네이션
- Todo Controller: 7개 핸들러 (201/200/204 응답)
- Todo Routes: 모든 라우트 authMiddleware 적용
- 단위 테스트 25개 + 통합 테스트 28개
- **137 tests 전체 통과**

---

### 25. Swagger UI 연동

> **`[프롬프트]`** swagger.json이 적용 되었는지 확인하고 그렇지 않으면 적용해줘.

`swagger-ui-express` 설치 + app.ts에 `/api-docs` 경로로 Swagger UI 연동.

---

### 26. Express 5 req.user 타입 에러 수정

> **`[프롬프트]`** (ts-node 컴파일 에러 보고)

Express 5에서 `express.d.ts` 타입 확장이 ts-node에서 인식되지 않는 문제:
- `express-serve-static-core` 모듈 확장으로 변경
- 최종 해결: `(req as any).user` 캐스팅 방식 적용
- **137 tests 전체 통과** 유지

---

### 27. 코드-문서-Swagger 일관성 검토 및 수정

> **`[프롬프트]`** backend 디렉토리의 코드와 docs 디렉토리의 문서들, @swagger/swagger.json 파일의 일치 여부를 검토하고, 차이가 있다면 문서를 업데이트 해줘.

**business-analyst 서브에이전트**가 12건 불일치 발견 → 전체 수정:

| 심각도 | 수정 내용 |
|--------|-----------|
| 높음 | DELETE 응답 200→204, 로그인 accessToken→token |
| 중간 | 로그아웃 메시지 통일, 로그인 400 응답 제거, 미들웨어 구조/역할 정의 수정 |
| 낮음 | 에러 메시지 통일, Express 5 + TypeScript 버전 명시 |

---

### 28. 스타일 가이드 작성

> **`[프롬프트]`** (99designs 웹사이트 캡처 이미지 첨부) 이 캡쳐된 화면의 스타일을 분석해서 프론트엔드의 스타일 가이드를 만들어줘. 파일은 9-style-guide.md 로 저장해줘.

캡처 화면 분석 → `docs/9-style-guide.md` 작성:
- 디자인 철학 (클린/미니멀, 카드 기반, 모노톤+레드 포인트)
- 색상 시스템 (Primary #DC3545, 중립 9종, 상태 4종)
- 타이포그래피 (Pretendard, 7단계 스케일)
- 컴포넌트 스타일 (Button 5종, Input 4상태, Card, Modal 등)
- CSS 변수 종합 (바로 사용 가능한 전체 변수 목록)

---

### 29. FE-01 프론트엔드 프로젝트 초기화

> **`[프롬프트]`** `/frontend-resolver` FE-01

**frontend-developer 서브에이전트**가 구현:
- Vite + React 19 + TypeScript 프로젝트 생성
- Zustand, TanStack Query, React Router, Axios 설치
- TypeScript 절대 경로 (`@/*` → `src/*`)
- ESLint + Prettier 설정
- Vitest 테스트 환경 구성
- CSS 변수 적용 (9-style-guide.md 기반)
- 디렉토리 구조 생성 (api/, components/, hooks/, pages/, stores/, types/, utils/)
- **1 test 통과**

---

### 30. FE-02 ~ FE-05 기반 모듈

> **`[프롬프트]`** 진행해줘.

**frontend-developer 서브에이전트**가 구현:
- FE-02: TypeScript 타입 정의 (auth, todo, api 제네릭)
- FE-03: Axios API 클라이언트 + 인터셉터 (auth 3 + todo 7)
- FE-04: Zustand 인증 스토어 (메모리 전용, localStorage 미사용)
- FE-05: 유틸리티 (computeTodoStatus, 검증 함수 6종)
- **69 tests 전체 통과**

---

### 31. FE-06 ~ FE-08 훅/컴포넌트/라우터

> **`[프롬프트]`** 진행해줘.

**frontend-developer 서브에이전트**가 구현:
- FE-06: TanStack Query 커스텀 훅 (useAuth, useTodos + CRUD mutations)
- FE-07: 공통 UI 컴포넌트 9개 (Button, Input, TextArea, Modal, Pagination, Select, StatusBadge, Header, Layout)
- FE-08: React Router 설정 + PrivateRoute/PublicRoute 인증 가드
- **91 tests 전체 통과**

---

### 32. FE-09 ~ FE-12 4개 페이지 구현

> **`[프롬프트]`** 진행해줘.

**frontend-developer 서브에이전트 2회 실행:**

**FE-09 회원가입 + FE-10 로그인:**
- RegisterForm (이름/이메일/비밀번호 유효성, 409 중복 처리)
- LoginForm (빈값 차단, 401 통합 메시지, 계정 존재 여부 비노출)
- 97 tests 통과

**FE-11 할일 목록 + FE-12 할일 상세:**
- TodoFilter (상태 6종 + 정렬), TodoCreateForm, TodoItem (체크박스 토글, 삭제 Modal)
- TodoList (페이지네이션 연동), TodoDetailPage (수정 모드, 완료/삭제)
- **120 tests 전체 통과**

---

### 33. 사용자 정보 수정 기능 추가

> **`[프롬프트]`** 사용자정보를 수정하는 화면을 추가해줘.

**2개 서브에이전트 병렬 실행** (백엔드 + 프론트엔드):

**백엔드:**
- GET/PUT /api/auth/profile (프로필 조회/수정)
- PUT /api/auth/profile/password (비밀번호 변경)
- 157 tests 통과

**프론트엔드:**
- ProfilePage (프로필 수정 + 비밀번호 변경 섹션)
- /profile 라우트 + Header 사용자 이름 → 프로필 링크
- 125 tests 통과

---

### 34. Dark Mode 기능

> **`[프롬프트]`** Dark Mode 기능을 적용해줘.

**frontend-developer 서브에이전트**가 구현:
- Zustand 테마 스토어 (localStorage 저장, 시스템 설정 감지)
- `[data-theme="dark"]` CSS 변수 오버라이드
- Header에 🌙/☀️ 토글 버튼
- 13개 CSS 파일 하드코딩 색상 → CSS 변수로 교체
- **141 tests 전체 통과**

---

### 35. 다국어 (i18n) 기능

> **`[프롬프트]`** 다국어(한국어, 영어, 일본어) 처리 기능을 적용해줘.

**frontend-developer 서브에이전트**가 구현:
- 자체 경량 i18n 시스템 (외부 라이브러리 없음)
- 번역 파일 3개 (ko.ts, en.ts, ja.ts)
- Zustand locale 스토어 + localStorage 영속화
- `useTranslation()` 훅 (dot notation + placeholder 치환)
- Header에 KO/EN/JA 언어 전환 버튼
- 전체 컴포넌트 하드코딩 한국어 → t() 호출로 교체
- validation-utils 에러 메시지를 번역 키로 변경
- **174 tests 전체 통과**

---

## 프로젝트 최종 현황 요약

### 커밋 히스토리

| 커밋 | 날짜 | 내용 |
|------|------|------|
| `b689d78` | 03/31 | Initial commit |
| `1633413` | 04/01 | docs: 프로젝트 설계 문서 및 DB 스키마 추가 |
| `4ac8657` | 04/01 | chore: .claude 및 .qwen 설정 디렉토리 추가 |
| `087397e` | 04/01 | docs: 와이어프레임, SVG 레이아웃, Swagger API 명세 추가 |
| `30f8836` | 04/01 | docs: mockup 디렉토리 추가 |
| `c7ff3a6` | 04/01 | feat: DB-01~DB-07 완료 — 백엔드 DB 레이어 구현 |
| `39fbd4b` | 04/02 | feat: BE-01~BE-15 백엔드 API 전체 구현 및 문서 동기화 |
| `4c9b96a` | 04/02 | feat: FE-01~FE-13 프론트엔드 전체 구현 |
| `e2f19aa` | 04/02 | feat: 사용자 정보 수정 기능 추가 |
| `e07d414` | 04/02 | feat: Dark Mode 기능 추가 |
| `29b9229` | 04/02 | feat: 다국어(한국어/영어/일본어) 기능 추가 |
| `3b55c44` | 04/02 | chore: qwen 에이전트 설정 업데이트 |

### 테스트 현황

| 영역 | 프레임워크 | 테스트 수 |
|------|-----------|-----------|
| 백엔드 | Jest + ts-jest + Supertest | 157건 |
| 프론트엔드 | Vitest + React Testing Library | 174건 |
| **합계** | | **331건 전체 통과** |

### 생성된 문서

| 문서 | 파일명 |
|------|--------|
| 도메인 정의서 | docs/1-domain-definition.md (v1.1.1) |
| PRD | docs/2-prd.md (v1.0.1) |
| 사용자 시나리오 | docs/3-user-scenario.md (v1.0.0) |
| 프로젝트 구조 설계 | docs/4-project-structure.md (v1.0.0) |
| 아키텍처 다이어그램 | docs/5-arch-diagram.md (v1.1.0) |
| ERD | docs/6-erd.md (v1.0.0) |
| 실행계획서 | docs/7-execution-plan.md (v1.0.0) |
| 와이어프레임 | docs/8-wireframes.md (v1.0.0) |
| 스타일 가이드 | docs/9-style-guide.md (v1.0.0) |
| Swagger API 명세 | swagger/swagger.json |

### 활용한 서브에이전트

| 서브에이전트 | 활용 장면 |
|-------------|-----------|
| business-analyst | 도메인 정의서, PRD, 시나리오, 일관성 검토 |
| Plan | 프로젝트 구조 설계, 실행계획 수립 |
| ui-designer | 와이어프레임 |
| documentation-engineer | 아키텍처 다이어그램 |
| api-designer | Swagger 명세, API 일관성 검토 |
| backend-developer | DB 레이어, 백엔드 API 전체 |
| frontend-developer | 프론트엔드 전체, Dark Mode, 다국어 |
